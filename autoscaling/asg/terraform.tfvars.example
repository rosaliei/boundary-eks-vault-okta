# Copy to terraform.tfvars (gitignored). Defaults are fine for the first run.
# asg_min_size  = 1
# asg_max_size  = 6
# instance_type = "t3.micro"
# vault_addr    = "http://<internal-nlb>:8200"   # or HCP Vault private endpoint
