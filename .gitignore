# -----------------------------------------------------------------------------
# Terraform
# -----------------------------------------------------------------------------
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfstate.backup
*.lock.info
*.tfplan
crash.log

# State holds real secrets in cleartext - boundary/terraform.tfstate contains the
# Vault broker token, aws/terraform.tfstate the cluster CA. Never commit.

# -----------------------------------------------------------------------------
# Secrets and local variable files
# -----------------------------------------------------------------------------
*.tfvars
!*.tfvars.example
vault-sa-token.txt
# contains a live Excalidraw+ API key
.mcp.json
*.pem
*.key
kubeconfig*
eks-ca.crt

# -----------------------------------------------------------------------------
# Local runs / OS
# -----------------------------------------------------------------------------
claude-run.log
.DS_Store
ca.crt

# Saved plan files (extensionless, so *.tfplan misses them)
tfplan*

# Packer build output (autoscaling/packer)
manifest.json
__pycache__/

# Terminal captures from real builds - kept locally, not committed
docs/run-logs/
